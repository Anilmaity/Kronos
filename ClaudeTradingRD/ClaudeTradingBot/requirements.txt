requests
python-dotenv
numpy
# Windows-only: native MT5 terminal bridge. On Linux (AWS ECS/EC2) this is
# skipped and the bot must run with BROKER=metaapi.
MetaTrader5>=5.0.5735 ; sys_platform == "win32"
